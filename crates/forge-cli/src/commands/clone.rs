use anyhow::Result;

pub fn run(_url: String, _path: Option<String>) -> Result<()> {
    // TODO: implement clone (init + pull)
    println!("clone: not yet implemented (requires server)");
    Ok(())
}
