# Forge Server Configuration (Docker)
# ====================================
# Mount your data volume to /data

[server]
listen = "0.0.0.0:9876"
max_message_size = 268435456
workers = 0

[storage]
base_path = "/data"
db_path = "forge.db"
